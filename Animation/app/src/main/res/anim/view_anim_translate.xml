<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillAfter="true"
    android:interpolator="@android:anim/linear_interpolator"
    android:repeatMode="reverse"
    android:shareInterpolator="true">

    <!--
    fillAfter:动画结束时，是否保持动画最后的状态
    fillBefore:动画结束时，是否还原到开始动画前的状态
    只能用在<set>里面
     -->

    <translate
        android:duration="3000"
        android:fromXDelta="50%"
        android:fromYDelta="-100%"
        android:repeatCount="1"
        android:startOffset="1000"
        android:toXDelta="20%p"
        android:toYDelta="20%p" />

    <!-- fromXDelta：起始点X轴坐标，
    这里表示的是你要滑出来的界面View的右上角的位置，可以是数值、百分数、百分数p。如果是数值，比如100，表示在View右上角向右边加上100px，
    如果是-100，表示在View的右上角加上-100px，也就是向左移。
    如果是百分数，就是按View自身的比例来进行平移的。
    如果是百分数p，是按照主界面的比例来进行平移的，这里的主界面指的是被View压住的界面。-->

    <!--
    android:toXDelta="100%",表示自身的100%,也就是从View自己的位置开始。
    android:toXDelta="80%p",表示父层View的80%,是以它父层View为参照的。
     -->

    <!-- 数值为正表示往右移动，数值为负表示往左移动 -->
</set>